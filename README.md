# 검색어 자동완성 시스템

> Trie 자료구조와 Redis 캐싱을 활용한 실시간 검색어 자동완성 시스템

![Java](https://img.shields.io/badge/Java-21-orange.svg)
![Spring Boot](https://img.shields.io/badge/Spring%20Boot-3.2.0-brightgreen.svg)
![Redis](https://img.shields.io/badge/Redis-7-DC382D.svg)
![Thymeleaf](https://img.shields.io/badge/Thymeleaf-3.1-005F0F.svg)

## 📚 기술 스택

- Java 21, Spring Boot 3.2.0
- Redis 7
- Thymeleaf, Vanilla JavaScript
- Docker Compose
- Gradle 8.5

## ✨ 기능 요구사항

### 핵심 기능
- **실시간 자동완성**: 300ms 디바운싱 적용, 최대 10개 추천
- **인기도 기반 정렬**: 검색 빈도수 기반 추천 우선순위
- **검색 기록 추적**: Redis에 검색 통계 저장 및 빈도수 관리
- **캐싱 전략**: Redis Cache-Aside 패턴, 10분 TTL

### 부가 기능
- **인기 검색어 Top 10**: 실시간 업데이트
- **검색 히스토리**: LocalStorage 기반 최근 10개 저장
- **실시간 통계**: 총 검색/오늘 검색/캐시된 쿼리 표시
- **키보드 네비게이션**: 방향키, Enter, ESC 지원

## 🤔 구현 시 고민사항

### 1. 자료구조 선택: Trie vs Elasticsearch

- **선택**: Trie 자료구조
- **Elasticsearch 선택 시**:
  - 고급 기능 (오타 교정, 동의어 처리, 분산 처리)
  - 대용량 데이터 처리 능력
- **Trie 선택 시**:
  - 응답 속도 50ms (Elasticsearch는 100ms+)
  - 간단한 구조 (인프라 비용 $0 vs $500/월)
- **선택 이유:** 소규모 프로젝트(~1000개 검색어)에서는 **속도와 단순함**이 중요함

### 2. 캐싱 전략: Cache-Aside vs Write-Through

- **선택**: Cache-Aside 패턴
- **Write-Through 선택 시**:
  - 캐시와 데이터베이스의 일관성
  - 첫 요청의 빠른 응답 (Cold Start)
- **Cache-Aside 선택 시**:
  - 메모리 효율 (필요한 것만 캐싱)
  - 쓰기 성능 향상 (캐시 업데이트 부담 없음)
  - 간단한 구현 (`@Cacheable`)
- **선택 이유**: 검색어 추천은 **몇 초 지연이 허용**되고, 읽기가 쓰기보다 훨씬 많기 때문임

### 3. 데이터 영속성: 단일 DB vs 3단계 저장소

- **선택**: Memory (Trie) + Redis + LocalStorage
- **단일 DB 선택 시**:
  - 단순한 아키텍처 (DB 하나만 사용)
  - 낮은 복잡도
- **3단계 저장소 선택 시**:
  - 초고속 응답 (메모리: ~1ms, Redis: ~5ms, DB: ~50ms)
  - 서버 재시작 시에도 데이터 유지 (Redis AOF)
  - 클라이언트 캐싱으로 서버 부하 감소
- **선택 이유:** 각 계층의 장점을 모두 활용함 (속도 + 영속성 + 서버 부하 감소)

### 4. 프론트엔드 최적화: 즉시 요청 vs 디바운싱 300ms

- **선택**: 300ms 디바운싱
- **즉시 요청 선택 시**:
  - 키 입력 즉시 결과 표시 (0ms 반응)
- **디바운싱 300ms 선택 시**:
  - API 호출 약 70% 감소 (7번 → 1번)
  - 서버 부하 대폭 감소
  - 불필요한 깜빡임 제거
- **선택 이유:** 인간은 **300ms 이하를 거의 인지하지 못함**

```
사용자 입력: "스" → "프" → "링"
- 즉시 요청: 3번 API 호출
- 디바운싱: 1번 API 호출 (입력 멈춘 후 300ms 후)
```

### 5. 데이터 집계: 실시간 vs 배치

- **선택**: 실시간 집계
- **배치 선택 시**:
  - 서버 부하 최소화 (배치는 1일 1번만 처리)
  - 복잡한 분석 (CTR, 전환율 등)
- **실시간 선택 시**:
  - 검색 직후 즉시 통계 반영
  - 사용자에게 피드백 제공 (알림)
- **선택 이유:** 소규모 시스템에서는 **즉각적인 반응**이 사용자 경험에 더 중요함

## 📊 트레이드오프 요약

| 결정 | 포기한 것 | 얻은 것               | 선택 이유        |
|------|----------|--------------------|--------------|
| Trie | 고급 기능, 확장성 | **속도, 단순함**        | 소규모는 단순함이 나음 |
| Cache-Aside | 완벽한 일관성 | **메모리 효율**         | 몇 초 지연 허용 가능 |
| 3단계 저장소 | 단순한 구조 | **속도 + 영속성**       | 각 계층의 장점 활용  |
| 300ms 디바운싱 | 즉각 반응 | **서버 부하 약 70% 감소** | 사용자는 못 느낌    |
| 실시간 집계 | 서버 효율 | **즉각적 피드백**        | 소규모는 실시간이 나음 |

1. **사용자 경험 > 기술적 완벽함**
2. **단순함 > 고급 기능**
3. **속도 > 메모리**

## 🧪 테스트 시나리오

1. "스프링" 입력 → 자동완성 확인
2. 방향키 ↑↓로 선택 → Enter로 검색
3. 인기 검색어 클릭 → 자동 검색
4. 검색 히스토리 확인
5. 통계 숫자 증가 확인

## 📖 참고 자료

- [가상 면접 사례로 배우는 대규모 시스템 설계 기초 - 13장](https://www.yes24.com/Product/Goods/102819435)
- [Spring Cache Documentation](https://docs.spring.io/spring-framework/reference/integration/cache.html)
- [Trie Data Structure](https://en.wikipedia.org/wiki/Trie)